import time
def mock_n_plus_one():
    start = time.time()
    # Mock generating employees excel
    for i in range(100):
        # 100 employees
        # 2 queries per employee
        pass
    print(f"Time: {time.time() - start}")

mock_n_plus_one()
